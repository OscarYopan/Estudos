// String, Number, Undefined, Null, Boolean, Symbol(vamos ver no futuro)

const nome = 'Oscar' //string
const idade = 35 // number
let anNascimento // undefined - não aponta pra local nenhum na memória
let sobrenome = null //nulo - não aponta pra local nenhum na memória
const aprovado = true // Boolean - só pode ser 'true' or 'false' valor lógico

console.log(typeof aprovado) // typeof para verificar o tipo